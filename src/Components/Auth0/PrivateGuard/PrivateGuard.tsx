// import { Navigate, Outlet } from "react-router-dom";
// import { useAuth0 } from "@auth0/auth0-react";

export const PrivateGuard = () =>{
    // const {user} = useAuth0();
    // const role = user['https://my-app.example.com'];

    // return role.includes('administrador') ? <Outlet/> : <Navigate to="/login" replace />
}