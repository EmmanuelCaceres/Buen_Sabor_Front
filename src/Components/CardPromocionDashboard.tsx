/*import React, { useState } from 'react'
import useModal from '../Hooks/useModal';*/
/*import IPromocion from '../Entities/IPromocion';*/

const CardPromocionDashboard = (/*props: { data: IPromocion }*/) => {
    /*const [isOpenModal, openModal, closeModal] = useModal(false);
    const [isOpenModalAdd, openModalAdd, closeModalAdd] = useModal(false);

    const [hover, setHover] = useState(false);

    const handleHover = () => {
        setHover(!hover);
    }

    return (
        <div className="cardEmpresa">
            <div>
                <img />
            </div>
            <div className="cardEmpresaBody">
                <div className="cardEmpresaBody-title">
                </div>
            </div>
        </div>
    )*/
}

export default CardPromocionDashboard
